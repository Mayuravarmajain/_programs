// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;


int main() {

    int n;
   cout<<"Enter the number of array element"<<endl;
   cin>>n;

   int arr[n];
   //array elemnts taking from the user
   cout <<n<<" array elements are"<<endl;
   for(int i=0;i<n;i++)
   {
       cout<<"Element "<<i+1<<" is ::";
       cin>>arr[i];
   }

   //displaying the arrya element
   cout<<"aray elements are"<<endl;
   for(int i=0;i<n;i++)
   {
       cout<<arr[i]<<" ";
   }


    return 0;
}
